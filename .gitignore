# Python
__pycache__/
*.pyc
.venv/
venv/

# Data (large, don't commit -- download separately per README)
data/*.csv
data/*.tar.gz
data/KuaiRand-Pure/
data/KuaiRand-1K/
data/KuaiRand-27K/

# Generated run artifacts (large binary prediction dumps / CatBoost's own training logs --
# logs/*.json summaries are the human-readable record and are committed)
logs/*.jsonl
outputs/*.json
outputs/*.npy
catboost_info/

# IDE
.vscode/
.DS_Store
